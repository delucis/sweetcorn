---
"@sweetcorn/astro": minor
---

Adds support for dithering SVG input files

Sharp’s SVG rendering only supports a subset of SVG features, so if an SVG isn’t working well, remove the `dither` attribute.
