---
title: Getting Started
description: Learn how to use Sweetcorn to dither images in your JavaScript projects
---

import { Steps } from '@astrojs/starlight/components';
import { PackageManagers } from 'starlight-package-managers';

Sweetcorn is a collection of JavaScript packages that dither images using [Sharp](https://sharp.pixelplumbing.com/).
You can use it directly to process images, or via a framework integration.

## Direct usage

<Steps>

1. Install `sweetcorn` and `sharp`:

   <PackageManagers type="add" pkg="sweetcorn sharp" />

2. Load an image with `sharp`, dither it with `sweetcorn`, and save the result:

   ```js
   import sharp from 'sharp';
   import sweetcorn from 'sweetcorn';

   const inputImage = await sharp('input.png');
   const ditheredImage = await sweetcorn(inputImage, {
   	algorithm: 'floyd-steinberg',
   });
   ditheredImage.webp({ lossless: true }).toFile('output.webp');
   ```

</Steps>

## Framework integrations

Sweetcorn also provides integrations for popular web frameworks:

- [Astro integration](/sweetcorn/frameworks/astro/)
